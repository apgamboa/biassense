# setup.py
from setuptools import setup

setup(name='biassense',
      description="package description",
      packages=["bias_sense"],
      version="0.0.1",) # You can have several packages, try it